/**
 * The single Firebase project identifier the Local Arena is permitted to bind
 * to. Mirrors `LOCAL_PROJECT_ID` in the server environment guard so tooling and
 * runtime cannot drift onto different targets (Section 1.11.8).
 */
export const LOCAL_PROJECT_ID = 'hallucinated-dungeons-local';
