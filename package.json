{
  "name": "hallucinated-dungeons-a3-toolchain",
  "version": "0.0.0",
  "private": true,
  "description": "Alpha 3 Phase 0 pinned development toolchain and Local Arena (Firebase Emulator Suite) harness. Contains no product/application code.",
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "verify:toolchain": "node scripts/verify-toolchain.mjs",
    "arena:start": "firebase emulators:start --project hallucinated-dungeons-local",
    "arena:validate": "node scripts/validate-arena.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "firebase-tools": "^13.29.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
